"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const chai_1 = require("chai");
const rxjs_1 = require("rxjs");
const socket_events_host_factory_1 = require("../socket-events-host-factory");
describe('SocketEventsHostFactory', () => {
    describe('create', () => {
        it(`should return expected observable socket object`, () => {
            const server = { test: 'test' };
            const result = socket_events_host_factory_1.SocketEventsHostFactory.create(server);
            chai_1.expect(result).to.have.keys('init', 'connection', 'disconnect', 'server');
            chai_1.expect(result.init instanceof rxjs_1.ReplaySubject).to.be.true;
            chai_1.expect(result.connection instanceof rxjs_1.Subject).to.be.true;
            chai_1.expect(result.disconnect instanceof rxjs_1.Subject).to.be.true;
            chai_1.expect(result.server).to.be.eql(server);
        });
    });
});
